<!-- app/views/layouts/pdf.html.erb -->
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Prescrição Nutricional</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    html, body {
    height: 100%;
    min-height: 100%;
    background: #121212 !important; /* fundo dark */
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      font-size: 11px;
      line-height: 1.5;
      color: #2c3e50;
      background: #121212;
      padding: 15px;
       min-height: 100vh; /* ocupa 100% da viewport do PDF */
    }
    
    /* HEADER */
    .header {
      text-align: center;
      margin-bottom: 25px;
      padding: 20px;
      background: linear-gradient(135deg, #3498db, #2980b9);
      color: white;
      border-radius: 12px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.1);
    }
    
    .header h1 {
      font-size: 24px;
      margin-bottom: 5px;
      font-weight: 600;
    }
    
    .header p {
      font-size: 14px;
      opacity: 0.9;
      font-weight: 300;
    }
    
    /* CLIENT INFO */
    .client-info {
      background: #0d0d0eff;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 20px;
      border-left: 4px solid #3498db;
    }
    
    .client-details h2 {
      color: #ffffffff;
      margin-bottom: 15px;
      font-size: 20px;
      font-weight: 600;
    }
    
    .info-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      margin-bottom: 15px;
    }
    
    .info-item {
      flex: 1;
      min-width: 200px;
      font-size: 12px;
      color: white;
    }
    
    .client-notes {
      margin-top: 15px;
      padding: 12px;
      background: #e8f4f8;
      color: #2c3e50;
      border-left: 4px solid #2980b9;
      border-radius: 6px;
      font-size: 11px;
    }
    
    /* INSTRUCTIONS */
    .instructions {
      background: #fff9e6;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 25px;
      border-left: 4px solid #f39c12;
    }
    
    .instructions h3 {
      color: #e67e22;
      margin-bottom: 10px;
      font-size: 14px;
    }
    
    .instructions ul {
      padding-left: 20px;
    }
    
    .instructions li {
      margin-bottom: 5px;
      font-size: 10px;
    }
    
    /* MEALS CONTAINER */
    .meals-container {
      margin-bottom: 30px;
    }
    
    .meal-section {
      margin-bottom: 20px;
      border: 1px solid #e0e6ed;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
      break-inside: avoid;
    }
    
    .meal-header {
      background: linear-gradient(135deg, #27ae60, #2ecc71);
      color: white;
      padding: 12px 18px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .meal-title {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    
    .meal-icon {
      font-size: 16px;
    }
    
    .meal-name {
      font-weight: 600;
      font-size: 14px;
    }
    
    .meal-time {
      background: rgba(255,255,255,0.2);
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 500;
    }
    
    .meal-content {
      padding: 18px;
      background: white;
    }
    
    .food-list {
      display: grid;
      gap: 8px;
    }
    
    .food-item {
      display: flex;
      align-items: center;
      padding: 8px 12px;
      background: #f8fafb;
      border-radius: 6px;
      border-left: 3px solid #3498db;
    }
    
    .quantity {
      background: #3498db;
      color: white;
      padding: 3px 8px;
      border-radius: 12px;
      font-weight: 600;
      font-size: 10px;
      margin-right: 12px;
      min-width: 40px;
      text-align: center;
    }
    
    .food-name {
      font-weight: 500;
      color: #2c3e50;
      font-size: 12px;
    }
    
    .meal-notes {
      margin-top: 12px;
      padding: 10px;
      background: #e8f5e8;
      border-radius: 6px;
      font-size: 10px;
      color: #27ae60;
    }
    
    .empty-meal {
      text-align: center;
      padding: 20px;
      color: #ffffffff;
      font-style: italic;
      background: #fafbfc;
      border-radius: 6px;
    }
    
    /* FOOTER */
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 2px solid #e0e6ed;
    }
    
    .nutritionist-info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 20px;
      gap: 30px;
    }
    
    .nutritionist-details,
    .contact-info {
      flex: 1;
      color: white;
    }
    
    .nutritionist-info h4 {
      color: #ffffffff;
      margin-bottom: 8px;
      font-size: 12px;
      font-weight: 600;
    }
    
    .nutritionist-info p {
      margin: 3px 0;
      font-size: 10px;
    }
    
    .disclaimer {
      background: #fff5f5;
      padding: 15px;
      border-radius: 8px;
      border-left: 4px solid #e74c3c;
      margin-bottom: 20px;
    }
    
    .disclaimer p {
      font-size: 9px;
      line-height: 1.4;
      color: #c0392b;
    }
    
    .signature-area {
      display: flex;
      justify-content: space-between;
      align-items: end;
      margin-top: 30px;
      color: white;
    }
    
    .signature-line,
    .date-line {
      text-align: center;
    }
    
    .signature-line p {
      margin: 5px 0;
      font-size: 10px;
    }
    
    .date-line p {
      font-size: 11px;
      font-weight: 600;
    }
    
    /* NO MEALS STATE */
    .no-meals {
      text-align: center;
      padding: 40px 20px;
      margin: 30px 0;
    }
    
    .empty-state {
      background: #f8fafb;
      padding: 30px;
      border-radius: 12px;
      border: 2px dashed #bdc3c7;
    }
    
    .empty-state h3 {
      color: #ffffffff;
      margin-bottom: 15px;
      font-size: 16px;
    }
    
    .empty-state p {
      color: #ffffffff;
      margin: 8px 0;
      font-size: 12px;
      line-height: 1.5;
    }
    
    /* PAGE BREAK */
    .page-break {
      page-break-before: always;
    }
    /* SUBSTITUIÇÕES DE ALIMENTOS */
.food-item-container {
  margin-bottom: 12px;
}

.food-item-container:last-child {
  margin-bottom: 0;
}

/* Ajuste no food-item quando tem substituições */
.food-item-container .food-item {
  margin-bottom: 0;
  border-left-width: 3px;
}

.food-item-container:has(.substitutions) .food-item {
  border-left-color: #27ae60;
  background: #f0f8f0;
}

.substitutions {
  margin-top: 8px;
  margin-left: 15px;
  background: #fafbfc;
  border: 1px solid #e0e6ed;
  border-radius: 6px;
  padding: 10px;
}

.substitution-header {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-bottom: 8px;
  font-size: 10px;
  color: #ffffffff;
  font-weight: 600;
}
.meals-page {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-height: 100vh; /* ocupa a folha inteira */
}
.substitution-icon {
  font-size: 12px;
  color: #27ae60;
}

.substitution-label {
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-size: 9px;
}

.custom-substitutions  {
  color: white !important;
  padding: 6px 10px;
  border-radius: 4px;
}

.substitutions-compact {
    display: flex;
    flex-direction: column;
    gap: 12px;
    color: white;
}

.substitution-list {
  display: grid;
  gap: 6px;
}

.substitution-item {
  display: flex;
  align-items: center;
  padding: 6px 8px;
  background: white;
  border-radius: 4px;
  border-left: 2px solid #27ae60;
}

.sub-quantity {
  background: #27ae60;
  color: white;
  padding: 2px 6px;
  border-radius: 10px;
  font-weight: 600;
  font-size: 9px;
  margin-right: 10px;
  min-width: 35px;
  text-align: center;
}

.sub-name {
  font-size: 11px;
  color: #ffffffff;
}

//* SEÇÃO DE SUBSTITUIÇÕES */
.substitutions-section {
  margin-top: 30px;
  padding: 20px;
  background: #f8fafb;
  border-radius: 10px;
}

.substitutions-header {
  text-align: center;
  margin-bottom: 25px;
  padding: 15px;
  background: #2c3e50;
  color: white;
  border-radius: 8px;
}

.substitutions-header h2 {
  font-size: 16px;
  margin-bottom: 5px;
  font-weight: 600;
  letter-spacing: 1px;
}

.substitutions-header p {
  font-size: 11px;
  opacity: 0.9;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.warning-box {
  background: #fff3cd;
  border: 2px solid #ffc107;
  color: #856404;
  padding: 10px;
  text-align: center;
  margin-bottom: 20px;
  border-radius: 6px;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.5px;
}

.substitution-table {
  margin-bottom: 20px;
  break-inside: avoid;
}

.table-title {
  background: #34495e;
  color: white;
  padding: 8px 15px;
  font-size: 12px;
  font-weight: 600;
  margin: 0;
  border-radius: 6px 6px 0 0;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.substitution-table table {
  width: 100%;
  border-collapse: collapse;
  background: white;
  border: 1px solid #dee2e6;
  border-top: none;
  border-radius: 0 0 6px 6px;
  overflow: hidden;
}

.substitution-table td {
  padding: 8px 10px;
  border: 1px solid #dee2e6;
  font-size: 9px;
  text-align: left;
  vertical-align: top;
  line-height: 1.3;
  min-height: 30px;
}

.substitution-table tr:nth-child(even) {
  background: #f8f9fa;
}

.substitution-table td:empty {
  background: #fafbfc;
}

/* Destacar quantidades */
.substitution-table td {
  position: relative;
}

.substitution-table td:not(:empty)::before {
  content: "•";
  color: #27ae60;
  font-weight: bold;
  margin-right: 4px;
}

/* Ajustes para diferentes categorias */
.substitution-table:has(.table-title:contains("PROTEÍNAS")) td {
  font-size: 8px;
  padding: 10px;
}

.substitution-table:has(.table-title:contains("VEGETAIS A")) {
  background: #e8f5e9;
}

.substitution-table:has(.table-title:contains("VEGETAIS A")) .table-title {
  background: #27ae60;
}

.substitution-table:has(.table-title:contains("PROTEÍNAS")) {
  background: #fff3e0;
}

.substitution-table:has(.table-title:contains("PROTEÍNAS")) .table-title {
  background: #e65100;
}

.substitution-table:has(.table-title:contains("FRUTAS")) {
  background: #fce4ec;
}

.substitution-table:has(.table-title:contains("FRUTAS")) .table-title {
  background: #c2185b;
}

.substitution-table:has(.table-title:contains("DERIVADOS")) {
  background: #e3f2fd;
}

.substitution-table:has(.table-title:contains("DERIVADOS")) .table-title {
  background: #1565c0;
}

/* Print optimizations */
@media print {
  .substitutions-section {
    page-break-before: always;
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }
  
  .substitution-table {
    break-inside: avoid;
  }
  
  .substitutions-header {
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }
  
  .warning-box {
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }
}
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>